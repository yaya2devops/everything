import { EmailRecipient } from './IncomingRecommendationService';
import { IncomingRecommendation } from './IncomingRecommendationService';
type StaffService = {
    api: {
        emails: {
            renderHTML(template: string, data: unknown): Promise<string>;
            renderText(template: string, data: unknown): Promise<string>;
        };
    };
};
export declare class IncomingRecommendationEmailRenderer {
    #private;
    constructor({ staffService }: {
        staffService: StaffService;
    });
    renderSubject(recommendation: IncomingRecommendation): Promise<string>;
    renderHTML(recommendation: IncomingRecommendation, recipient: EmailRecipient): Promise<string>;
    renderText(recommendation: IncomingRecommendation, recipient: EmailRecipient): Promise<string>;
}
export {};
