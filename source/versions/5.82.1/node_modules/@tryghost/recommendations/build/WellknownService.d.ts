/// <reference types="node" />
import { Recommendation } from './Recommendation';
type UrlUtils = {
    relativeToAbsolute(url: string): string;
};
type Options = {
    /**
     * Where to publish the wellknown file
     */
    dir: string;
    urlUtils: UrlUtils;
};
export declare class WellknownService {
    #private;
    dir: string;
    urlUtils: UrlUtils;
    constructor({ dir, urlUtils }: Options);
    getPath(): string;
    getURL(): URL;
    set(recommendations: Recommendation[]): Promise<void>;
}
export {};
