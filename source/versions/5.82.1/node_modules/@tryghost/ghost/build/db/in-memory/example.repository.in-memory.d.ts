import { Greeting } from '../../core/example/example.entity';
import { ExampleRepository } from '../../core/example/example.repository';
interface DomainEvents {
    dispatch(event: unknown): void;
}
export declare class ExampleRepositoryInMemory implements ExampleRepository {
    private readonly events;
    constructor(events: DomainEvents);
    getOne(greeting: string): Promise<Greeting>;
    save(entity: Greeting): Promise<void>;
}
export {};
