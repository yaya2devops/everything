import { ExampleRepository } from './example.repository';
export declare class ExampleService {
    private readonly repository;
    constructor(repository: ExampleRepository);
    greet(recipient: string): Promise<string>;
}
