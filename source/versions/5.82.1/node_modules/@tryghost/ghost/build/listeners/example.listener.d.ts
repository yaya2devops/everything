import { ExampleEvent } from '../core/example/example.event';
interface Logger {
    info(message: string): void;
}
export declare class ExampleListener {
    private logger;
    constructor(logger: Logger);
    logEvents(event: ExampleEvent): Promise<void>;
}
export {};
