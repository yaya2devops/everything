export type EmailAddress = {
    address: string;
    name?: string;
};
export declare class EmailAddressParser {
    static parse(email: string): EmailAddress | null;
    static stringify(email: EmailAddress): string;
}
